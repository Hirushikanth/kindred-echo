# Dependencies
node_modules/

# Next.js
.next/
out/
.vercel/

# Environment (never commit secrets; keep shared template tracked)
.env
.env.*
!.env.example

# Build / TypeScript / tests
dist/
coverage/
*.tsbuildinfo

# Turbopack cache (future-proof)
.turbo/

# OS junk
.DS_Store

# Logs / debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log

# Editors (optional junk only)
*.sw?
*~

# Playwright — reports/traces/screenshots/videos are heavy; keep repo lean (use Cursor MCP browser when needed).
test-results/
playwright-report/
blob-report/
playwright/.cache/
.playwright/

# Cursor / agent browser artifacts (if any MCP runner writes locally)
**/trace.zip

