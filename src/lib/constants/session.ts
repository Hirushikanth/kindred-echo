/** sessionStorage — consent acknowledgment timestamp (ISO) */
export const CONSENT_TS_STORAGE_KEY = "kindred_echo_consent_at";

/** HTTP-only cookie set by POST /api/rooms — current demo room session */
export const ROOM_COOKIE_NAME = "ke_room_id";
